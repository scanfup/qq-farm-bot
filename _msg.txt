fix: 日志改为同步写入，并加入卡死诊断

- 日志由 createWriteStream 改为 appendFileSync：原实现缓冲未落盘时
  进程被强杀会丢失全部内容（实测日志文件为 0 字节），且 exit 事件
  在强制中断时不会触发；同步写不经过事件循环，任何情况下都保留记录
- 记录启动参数、终端状态（TTY/raw/行列数）、cwd
- 记录全部按键事件，便于判断按键无响应是键盘问题还是渲染阻塞
- 每 30 秒记录一次存活心跳（含渲染次数与背压跳过次数）